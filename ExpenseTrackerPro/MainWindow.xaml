<Window x:Class="ExpenseTrackerPro.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:ExpenseTrackerPro" Title="ExpenseTracker Pro" Height="800" Width="1240" MinHeight="640" MinWidth="960" WindowStartupLocation="CenterScreen" Background="{StaticResource BackgroundBrush}">
  <Window.DataContext><local:MainViewModel /></Window.DataContext>
  <Grid Margin="28">
    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
    <Grid Grid.Row="0" Margin="0,0,0,24"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="ExpenseTracker Pro" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextBrush}" /><TextBlock Text="Buget personal, offline și local" Foreground="{StaticResource MutedBrush}" /></StackPanel><DatePicker Grid.Column="1" SelectedDate="{Binding SelectedDate}" Width="155" /></Grid>
    <StackPanel Grid.Row="1" Margin="0,0,0,20"><TextBlock Text="Control clar asupra banilor tăi." FontSize="29" FontWeight="Bold" Foreground="{StaticResource TextBrush}" /><TextBlock Text="Tranzacțiile sunt salvate în SQLite, pe acest calculator." Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0" /></StackPanel>
    <UniformGrid Grid.Row="2" Columns="4" Margin="0,0,0,16"><Border Style="{StaticResource CardStyle}" Margin="0,0,12,0"><StackPanel><TextBlock Text="VENITURI" Foreground="{StaticResource MutedBrush}" /><TextBlock Text="{Binding Income, StringFormat={}{0:N2} RON}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" /></StackPanel></Border><Border Style="{StaticResource CardStyle}" Margin="0,0,12,0"><StackPanel><TextBlock Text="CHELTUIELI" Foreground="{StaticResource MutedBrush}" /><TextBlock Text="{Binding Expenses, StringFormat={}{0:N2} RON}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource DangerBrush}" /></StackPanel></Border><Border Style="{StaticResource CardStyle}" Margin="0,0,12,0"><StackPanel><TextBlock Text="SOLD LUNAR" Foreground="{StaticResource MutedBrush}" /><TextBlock Text="{Binding Balance, StringFormat={}{0:N2} RON}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextBrush}" /></StackPanel></Border><Border Style="{StaticResource CardStyle}"><StackPanel><TextBlock Text="BUGET RĂMAS" Foreground="{StaticResource MutedBrush}" /><TextBlock Text="{Binding RemainingBudget, StringFormat={}{0:N2} RON}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextBrush}" /></StackPanel></Border></UniformGrid>
    <Grid Grid.Row="3"><Grid.ColumnDefinitions><ColumnDefinition Width="360" /><ColumnDefinition Width="16" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
      <Border Grid.Column="0" Style="{StaticResource CardStyle}" VerticalAlignment="Top"><StackPanel><TextBlock Text="Adaugă tranzacție" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" /><TextBlock Text="Tip" Foreground="{StaticResource MutedBrush}" Margin="0,18,0,6" /><ComboBox ItemsSource="{Binding TransactionTypes}" SelectedItem="{Binding NewType}" /><TextBlock Text="Categorie" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,6" /><ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding NewCategory}" /><TextBlock Text="Descriere" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,6" /><TextBox Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}" MaxLength="120" /><TextBlock Text="Sumă (RON)" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,6" /><TextBox Text="{Binding NewAmount, UpdateSourceTrigger=PropertyChanged}" /><TextBlock Text="Dată" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,6" /><DatePicker SelectedDate="{Binding NewDate}" /><Button Content="Salvează tranzacția" Command="{Binding AddTransactionCommand}" Background="{StaticResource AccentBrush}" Foreground="#082019" Margin="0,18,0,0" /><TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,12,0,0" /></StackPanel></Border>
      <Border Grid.Column="2" Style="{StaticResource CardStyle}"><DockPanel><StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Tranzacții" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" /><Button Content="Șterge selectata" Command="{Binding DeleteTransactionCommand}" Margin="16,0,0,0" /><Button Content="Export CSV" Command="{Binding ExportCsvCommand}" Margin="8,0,0,0" /></StackPanel><DataGrid ItemsSource="{Binding Transactions}" SelectedItem="{Binding SelectedTransaction}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"><DataGrid.Columns><DataGridTextColumn Header="Data" Binding="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" Width="120" /><DataGridTextColumn Header="Descriere" Binding="{Binding Description}" Width="*" /><DataGridTextColumn Header="Categorie" Binding="{Binding Category}" Width="150" /><DataGridTextColumn Header="Tip" Binding="{Binding Type}" Width="110" /><DataGridTextColumn Header="Sumă" Binding="{Binding Amount, StringFormat={}{0:N2} RON}" Width="140" /></DataGrid.Columns></DataGrid></DockPanel></Border>
    </Grid>
  </Grid>
</Window>
